<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PPAsta.Pages.BuyersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PPAsta.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Barra di ricerca -->
            <TextBox Grid.Column="0"
                 Text="{Binding TextResearch, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                 TextChanged="OnTextChanged"
                 Header="Ricerca"
                 PlaceholderText="Ricerca per nome, numero o anno"
                 Margin="0,0,10,0" />

            <!-- ComboBox filtro -->
            <ComboBox Grid.Column="1"
                  Header="Anno"
                  ItemsSource="{Binding FilterYears}"
                  SelectedItem="{Binding SelectedFilter, Mode=TwoWay}"
                  SelectionChanged="ComboBoxYears_SelectionChanged"
                  />
        </Grid>


        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Button x:Name="AddButton" 
                    Content="Aggiungi" 
                    Click="Add_Click"
                    HorizontalAlignment="Right">
            </Button>
        </Grid>

        <controls:DataGrid Grid.Row="2"
            ItemsSource="{Binding Buyers}"
            AutoGenerateColumns="False"
            IsReadOnly="True"
            Margin="0,10,0,0"
            Sorting="DataGrid_Sorting">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="Name" Tag="Name" Binding="{Binding Name}" />
                <controls:DataGridTextColumn Header="Numero" Tag="Number" Binding="{Binding Number}" />
                <controls:DataGridTextColumn Header="Anno" Tag="Year" Binding="{Binding Year}" />

                <controls:DataGridTemplateColumn Header="Modifica">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button
                                Click="Edit_Click"
                                Tag="{Binding}"
                                Margin="5"
                                MinWidth="80" >
                                <FontIcon Glyph="&#xE70F;" />
                            </Button>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>

                <controls:DataGridTemplateColumn Header="Modifica">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button
                                Click="Delete_Click"
                                Tag="{Binding}"
                                Margin="5"
                                MinWidth="80" >
                                <FontIcon Glyph="&#xe74d;" />
                            </Button>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>

            </controls:DataGrid.Columns>
        </controls:DataGrid>

        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" 
                   HorizontalAlignment="Left" 
                   VerticalAlignment="Center" 
                   Text="{Binding TotalRecordsText}" 
                   x:Name="RecordsCountText"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="PrevButton" Content="Indietro" Click="PrevButton_Click" Margin="0,0,10,0" />
                <TextBlock x:Name="PageInfoText" VerticalAlignment="Center" Text="{Binding PageText}" />
                <Button x:Name="NextButton" Content="Avanti" Click="NextButton_Click" Margin="10,0,0,0" />
            </StackPanel>
        </Grid>
    </Grid>

    <Page.Resources>
        <DataTemplate x:Key="BuyerDialog">
            <StackPanel Spacing="10">
                <TextBox PlaceholderText="Nome" x:Name="NameTextBox" />
                <TextBox PlaceholderText="Numero" x:Name="NumberTextBox" />
                <TextBox PlaceholderText="Anno" x:Name="YearTextBox" />
            </StackPanel>
        </DataTemplate>
    </Page.Resources>
</Page>
